# Backend (Node.js + Express)

This is a placeholder backend for TimeBoxed Peer Learning.

## Setup

1. Install dependencies:
   - `npm install`
2. Copy env example:
   - `copy .env.example .env`
3. Update `MONGODB_URI` in `.env` if needed.

## Run

- `npm run dev`

All endpoints return placeholder JSON responses for now.
